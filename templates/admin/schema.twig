{% extends "admin-dashboard.twig" %}

{% block dashcontent %}

<aside class="dash-content-sidebar">
	<header>
		<input type="search" class="filter" placeholder="Filter schemas" />
	</header>

	<nav class="links">
		<h3>Custom Schemas</h3>
		<ul>
			{% for schema in sortByKey(cms.customSchemas()) %}
			<li class="schema-{{schema.id}}">
				<a  {% if url.schema == schema.id %}class="active"{% endif %}
					href="/admin/schemas/{{ schema.id }}"
					title="{{ schema.description }}">{{ schema.id }}</a>
			</li>
			{% endfor %}
		</ul>
		<h3>Built-in Schemas</h3>
		<ul>
			{% for schema in sortByKey(cms.reservedSchemas()) %}
			<li class="schema-{{schema.id}}">
				<a  {% if url.schema == schema.id %}class="active"{% endif %}
					href="/admin/schemas/{{ schema.id }}"
					title="{{ schema.description }}">{{ schema.id }}</a>
			</li>
			{% endfor %}
		</ul>
	</nav>

	<footer>
		<a class="dash-button full transparent" href="/admin/schemas/new" title="Create a new schema">+ New Schema</a>
	</footer>
</aside>

<main class="dash-main-content">
	{% if url.schema is empty %}
		{% include "admin/schema-index.twig" %}
	{% else %}
		{% if url.schema == "new" %}
			<h1><a href="/admin/schemas">schemas</a> / <span>new</span></h1>
			{{ cms.form.schema() }}
		{% else %}
			<h1><a href="/admin/schemas">schemas</a> / <span>{{ url.schema }}</span></h1>
			{{ cms.form.schema({
				id     : url.schema,
				save   : 'Save',
			}) }}
		{% endif %}
	{% endif %}
</main>

{% endblock %}


