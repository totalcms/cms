{#--------------------------------
  Markdown Macro
--------------------------------#}
{% macro markdown(property, args = {}) %}
    {# TODO: create JS lib that adds toolbar + preview #}
	{# Override args with static values #}
	{% set args =  args | merge({
		field  : "markdown",
	}) %}
    {{ _self.textarea(property, args) }}
{% endmacro %}
