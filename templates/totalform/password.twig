{#--------------------------------
  Password Macro
--------------------------------#}
{% macro password(name, args = {}) %}
	{# Override args with static values #}
	{% set args =  args | merge({
		field : "password",
		type  : "password",
	}) %}
    {{ _self.input(name, args) }}
{% endmacro %}
