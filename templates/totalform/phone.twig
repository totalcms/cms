{#--------------------------------
  Telephone Macro
--------------------------------#}
{% macro phone(property, args = {}) %}
	{# Override args with static values #}
	{% set args =  args | merge({
		field : "phone",
		type  : "tel",
	}) %}
    {{ _self.input(property, args) }}
{% endmacro %}