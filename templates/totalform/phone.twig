{#--------------------------------
  Telephone Macro
--------------------------------#}
{% macro phone(name, args = {}) %}
	{# Override args with static values #}
	{% set args =  args | merge({
		field : "phone",
		type  : "tel",
	}) %}
    {{ _self.input(name, args) }}
{% endmacro %}