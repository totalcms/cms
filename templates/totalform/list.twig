{#--------------------------------
  List Macro
--------------------------------#}
{% macro list(name, options, args = {}) %}
	{# Override args with static values #}
	{% set args =  args | merge({
		field : "list",
		rows : 6,
	}) %}
    {{ _self.selectmultiple(name, options, args) }}
{% endmacro %}