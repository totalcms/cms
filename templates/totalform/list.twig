{#--------------------------------
  List Macro
--------------------------------#}
{% macro list(property, options, args = {}) %}
	{# Override args with static values #}
	{% set args =  args | merge({
		field : "list",
		rows : 6,
	}) %}
    {{ _self.multiselect(property, options, args) }}
{% endmacro %}