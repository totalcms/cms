{#--------------------------------
  Time Macro
--------------------------------#}
{% macro time(name, args = {}) %}
	{# Override args with static values #}
	{% set args =  args | merge({
		field : "time",
		type  : "time",
	}) %}
    {{ _self.input(name, args) }}
{% endmacro %}
