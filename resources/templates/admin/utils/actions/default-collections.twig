{# Skip blog-legacy and reference-only schemas (totalcms, totalcms-item) #}
{% set reservedSchemas = cms.schema.reserved() | filterCollection([
	{ property : "id", operator : "not-equal", value : "blog-legacy" },
	{ property : "id", operator : "not-equal", value : "totalcms" },
	{ property : "id", operator : "not-equal", value : "totalcms-item" },
]) %}

<p>Creating default collections...</p>
<ul>
{% for schema in reservedSchemas %}
	{% set collection = cms.collection.get(schema.id) %}
	<li>Created... {{ collection.name }}</li>
{% endfor %}
</ul>
<p>Done. Check out your <a href="collections">New Collections</a>.</p>