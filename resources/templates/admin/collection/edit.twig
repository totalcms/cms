{# Edit Collection #}
{{ cms.form.collection({
	id           : url.collection,
	save         : cms.isAdmin() ? "Save" : false,
	delete       : cms.isAdmin() ? "Delete" : false,
	helpStyle    : 'label',
	helpOnFocus  : true,
	deleteActions : [
		{
			"action" : "redirect",
			"link"   : "collections"
		}
	],
	editActions : [
		{
			"action" : "refresh",
		}
	],
}) }}
