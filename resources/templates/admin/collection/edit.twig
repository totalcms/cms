{# Edit Collection #}
{{ cms.form.collection({
	id           : url.collection,
	save         : cms.canAccessCollectionMeta(url.collection, 'update') ? "Save" : false,
	delete       : cms.canAccessCollectionMeta(url.collection, 'delete') ? "Delete" : false,
	helpStyle    : 'label',
	helpOnFocus  : true,
	deleteActions : [
		{
			"action" : "redirect",
			"link"   : "collections"
		}
	],
	editActions : [
		{
			"action" : "refresh",
		}
	],
}) }}
