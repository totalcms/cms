{# Edit Collection #}
{{ cms.form.collection({
	id           : url.collection,
	save         : "Save",
	delete       : "Delete",
	helpStyle    : 'label',
	helpOnFocus  : true,
	deleteActions : [
		{
			"action" : "redirect",
			"link"   : "collections"
		}
	],
	editActions : [
		{
			"action" : "refresh",
		}
	],
}) }}
