{# New Collection Form #}
<h1><a href="collections">collections</a> / <span>new</span></h1>

{{ cms.form.collection({
	save        : cms.isAdmin() ? "Save" : false,
	helpStyle   : 'label',
	helpOnFocus : true,
	newActions   : [
		{
			"action" : "redirect-object",
			"link"   : "collections/{id}/edit"
		}
	],
}) }}
