{# New Object Form #}
{% set canSave = cms.canAccess(url.collection, 'POST') %}
{% set form = cms.form.builder(url.collection, {
	save        : canSave ? "Save" : false,
	helpStyle   : 'label',
	helpOnFocus : true,
	newActions   : [
		{
			"action" : "redirect-object",
			"link"   : "collections/#{url.collection}/{id}"
		}
	],
}) %}
{{ form.autoBuild() }}