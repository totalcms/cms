{# New Object Form #}
{% set form = cms.form.builder(url.collection, {
	save        : "Save",
	helpStyle   : 'label',
	helpOnFocus : true,
	newAction   : {
		"action" : "redirect-object",
		"link"   : "collections/#{url.collection}/{id}"
	},
}) %}
{{ form.autoBuild() }}