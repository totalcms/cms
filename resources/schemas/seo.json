{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://www.totalcms.co/schemas/seo.json",
	"id": "seo",
	"type": "object",
	"description": "Per-object SEO overrides. Used as a card sub-schema on objects that opt in.",
	"category": "Internal",
	"formgrid": "title socialTitle\njsonldType canonical\ndescription description\nnoindex nofollow\nimage image",
	"properties": {
		"id": {
			"$ref": "https://www.totalcms.co/schemas/properties/slug.json",
			"label": "ID",
			"help": "Reserved — required by the schema system but unused for cards.",
			"field": "id",
			"factory": "slug",
			"settings": {
				"readonly": true
			}
		},
		"title": {
			"type": "string",
			"label": "Title",
			"help": "The page title. The site title template still applies.",
			"field": "text",
			"default": ""
		},
		"socialTitle": {
			"type": "string",
			"label": "Social Title",
			"help": "Short title for share cards (og:title / twitter:title). Leave empty to use the page title.",
			"field": "text",
			"default": ""
		},
		"description": {
			"type": "string",
			"label": "Description",
			"help": "Overrides the meta description for this object",
			"field": "textarea",
			"default": ""
		},
		"image": {
			"$ref": "https://www.totalcms.co/schemas/properties/image.json",
			"label": "Social Image",
			"help": "Social sharing image; 1200×630 recommended",
			"field": "image"
		},
		"canonical": {
			"$ref": "https://www.totalcms.co/schemas/properties/url.json",
			"label": "Canonical URL",
			"help": "Absolute URL. Leave empty to use this page's own URL",
			"field": "url",
			"default": ""
		},
		"noindex": {
			"type": "boolean",
			"label": "No Index",
			"help": "Ask search engines not to index this page; also removes it from sitemaps",
			"field": "toggle",
			"default": false
		},
		"nofollow": {
			"type": "boolean",
			"label": "No Follow",
			"help": "Ask search engines not to follow links from this page",
			"field": "toggle",
			"default": false
		},
		"jsonldType": {
			"type": "string",
			"label": "Structured Data Type",
			"help": "What this record is, for og:type and the JSON-LD. Automatic is the collection's Structured Data Type for an object, and Webpage for a Site Builder page (a page has no collection setting). Article and Blog post add an Article node and emit article:published_time.",
			"field": "select",
			"default": "",
			"options": [
				{
					"value": "",
					"label": "Automatic"
				},
				{
					"value": "website",
					"label": "Webpage"
				},
				{
					"value": "article",
					"label": "Article"
				},
				{
					"value": "blogposting",
					"label": "Blog post"
				}
			]
		}
	},
	"required": []
}
