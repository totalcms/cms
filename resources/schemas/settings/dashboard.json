{
	"$schema"     : "https://json-schema.org/draft/2020-12/schema",
	"title"       : "Dashboard Settings",
	"description" : "Admin dashboard configuration",
	"type"        : "object",
	"properties"  : {
		"title": {
			"type"        : "string",
			"field"       : "text",
			"label"       : "Admin Dashboard Browser Title",
			"help"        : "The browser title displayed in admin dashboard pages. This appears in the browser tab and bookmarks. Leave at the default to use \"{Site Name} Admin\" whenever a site name is set.",
			"placeholder" : "Total CMS Admin"
		},
		"pagination": {
			"type"        : "number",
			"label"       : "Dashboard Table Pagination",
			"help"        : "The number of items to display per page in the dashboard table. Set the value to <code>0</code> to disable pagination. When set to <code>0</code>, all items will be displayed on a single page.",
			"placeholder" : "50",
			"min"         : 0
		},
		"confirmCountdown": {
			"type"        : "number",
			"label"       : "Confirmation Countdown (seconds)",
			"help"        : "Destructive confirmation dialogs (delete, reset, etc.) disable the confirm button for this many seconds, forcing the user to pause and read the message. Set to <code>0</code> to disable the countdown.",
			"placeholder" : "5",
			"min"         : 0,
			"max"         : 30,
			"default"     : 5
		},
		"accent": {
			"type"    : "string",
			"field"   : "color",
			"label"   : "Dashboard Accent Color",
			"help"    : "The accent color used throughout the admin dashboard.",
			"default" : "#4d91e2"
		},
		"keepIdOnDuplicate": {
			"type"    : "boolean",
			"field"   : "toggle",
			"label"   : "Keep ID on Duplicate Object?",
			"help"    : "When enabled, duplicating an object will keep the original ID (user must change it manually before saving). When disabled (default), the ID field is cleared to allow autogen rules to work.",
			"default" : false
		},
		"fullWidthForms": {
			"type"    : "boolean",
			"field"   : "toggle",
			"label"   : "Full Width Object Forms",
			"help"    : "When enabled, forms in the admin dashboard expand to fill the full available width instead of being capped at 800px.",
			"default" : false
		},
		"inlineEditing": {
			"type"    : "boolean",
			"field"   : "toggle",
			"label"   : "Inline Editing",
			"help"    : "Master switch for editing values in place. Turning it off hides the pencil in every collection table and refuses inline saves for everyone, regardless of access groups. When it is on, access groups decide who may use it.",
			"default" : true
		},
		"moreMenu": {
			"type"     : "array",
			"items"    : { "type": "string" },
			"field"    : "checklist",
			"label"    : "More Menu",
			"help"     : "Sidebar items you rarely use. Checked items leave the sidebar and move into the <strong>More</strong> (&#8942;) menu at the bottom of it, where they stay one click away. Items added by extensions are listed here too.",
			"default"  : [],
			"settings" : {
				"fieldGrid"         : "250px",
				"propertyOptions"   : "navItems",
				"sortOptions"       : false,
				"mergeStoredValues" : false
			}
		}
	}
}
