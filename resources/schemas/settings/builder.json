{
	"$schema"     : "https://json-schema.org/draft/2020-12/schema",
	"title"       : "Site Builder Settings",
	"description" : "Configure the Site Builder feature",
	"type"        : "object",
	"properties"  : {
		"pagesCollection": {
			"type"        : "string",
			"field"       : "select",
			"label"       : "Pages Collection",
			"help"        : "The collection used for page metadata. Only collections whose schema is <code>builder-page</code>, or inherits from it, are offered — every object in this collection is served as a page.",
			"placeholder" : "builder-pages",
			"settings"    : {
				"propertyOptions" : "pageCollections",
				"sortOptions"     : true
			}
		},
		"assetsPath": {
			"type"        : "string",
			"field"       : "text",
			"label"       : "Assets Path",
			"help"        : "Public assets directory relative to docroot. Your build tool should output CSS, JS, and other assets here.",
			"placeholder" : "assets"
		},
		"liveReload": {
			"type"    : "boolean",
			"field"   : "toggle",
			"label"   : "Live Reload Preview",
			"help"    : "When you save a Builder template or page, every open admin tab on a Builder page reloads automatically. Only active for logged-in admin sessions; visitors never see it.",
			"default" : true
		}
	}
}
